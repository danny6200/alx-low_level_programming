#include<stdio.h>
/**
 * main - prints some string to stdout
 *
 * Return: 0 when the function executes successfully
 */
int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
