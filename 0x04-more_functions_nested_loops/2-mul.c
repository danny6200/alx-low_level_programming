#include "main.h"
/**
 * mul - returns the result for the mulplication of two integers
 * @a: integer to be multiplied
 * @b: integer to be multiplied
 *
 * Return: result
 */
int mul(int a, int b)
{
	int res = a * b;

	return (res);
}
